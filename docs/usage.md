# Usage

Default output path is `.bridgedai/context.json`.

```yaml
- uses: bridgedai-devsecops/collect-context-action@v1
  with:
    output-file: .bridgedai/context.json
    include-event-payload: 'false'
```

